{
  "name": "seufert-co",
  "version": "1.0.0",
  "private": true,
  "description": "A static personal blog for GitHub Pages.",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve --port=8080",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
